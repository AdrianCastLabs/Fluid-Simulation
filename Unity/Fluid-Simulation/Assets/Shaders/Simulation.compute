#pragma kernel ComputeGravity

RWStructuredBuffer<float3> positions;

int nParticles;
float gravity;

[numthreads(128,1,1)]
void ComputeGravity (uint3 id : SV_DispatchThreadID)
{
    if (id.x >= nParticles) return;
    positions[id.x].y -= 0.1 * gravity;
}
